# 인증키 - 절대 커밋하지 않는다
.env
*.env
!.env.example

# 수집 데이터 (용량이 크고 재수집 가능)
data/
logs/

# 파이썬
__pycache__/
*.py[cod]

# 등록 스크립트가 생성하는 런처 (파이썬 경로가 PC 마다 다르다)
scripts/run_collector.cmd

# 개인 설정
.claude/settings.local.json
